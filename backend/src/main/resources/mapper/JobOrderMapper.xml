<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.jobs.mapper.JobOrderMapper">

    <resultMap id="orderMap" type="com.campus.jobs.entity.JobOrder">
        <id property="id" column="id"/>
        <result property="jobId" column="job_id"/>
        <result property="applicantName" column="applicant_name"/>
        <result property="applicantPhone" column="applicant_phone"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="jobTitle" column="job_title"/>
        <result property="company" column="company"/>
    </resultMap>

    <sql id="filter">
        <where>
            <if test="keyword != null and keyword != ''">
                and (o.applicant_name like concat('%', #{keyword}, '%')
                     or o.applicant_phone like concat('%', #{keyword}, '%')
                     or j.title like concat('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                and o.status = #{status}
            </if>
        </where>
    </sql>

    <select id="selectList" resultMap="orderMap">
        select o.*, j.title as job_title, j.company as company
        from job_order o
        left join job j on o.job_id = j.id
        <include refid="filter"/>
        order by o.id desc
        limit #{offset}, #{limit}
    </select>

    <select id="count" resultType="long">
        select count(*)
        from job_order o
        left join job j on o.job_id = j.id
        <include refid="filter"/>
    </select>

    <select id="selectById" resultMap="orderMap">
        select o.*, j.title as job_title, j.company as company
        from job_order o
        left join job j on o.job_id = j.id
        where o.id = #{id}
    </select>

    <insert id="insert" parameterType="com.campus.jobs.entity.JobOrder" useGeneratedKeys="true" keyProperty="id">
        insert into job_order (job_id, applicant_name, applicant_phone, status, remark, create_time)
        values (#{jobId}, #{applicantName}, #{applicantPhone}, #{status}, #{remark}, now())
    </insert>

    <update id="update" parameterType="com.campus.jobs.entity.JobOrder">
        update job_order
        <set>
            <if test="jobId != null">job_id = #{jobId},</if>
            <if test="applicantName != null">applicant_name = #{applicantName},</if>
            <if test="applicantPhone != null">applicant_phone = #{applicantPhone},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from job_order where id = #{id}
    </delete>

</mapper>
