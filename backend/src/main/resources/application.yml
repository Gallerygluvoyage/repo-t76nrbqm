server:
  port: 8080

spring:
  application:
    name: campus-jobs
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_jobs?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: "123456"

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.campus.jobs.entity
  configuration:
    map-underscore-to-camel-case: true
